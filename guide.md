# First Principal: No guide, no .md files, no explainations. Just do what I tell you.
# Test stack: Frontend: Next.js + TailwindCSS; Backend: Rust.
# This is a Fintech Application: Bank Transfer, QR payment.
# This application will be deployed using Dokploy. With Dockerfile (production - prebuilt image pull from github CI/CD), and Dockerfile.build (build from scratch - for development).

Today's tasks:
 - Ready Dockerfile and Dockerfile.build for frontend and backend.
 - Build backend using Dokploy's Database: Postgres 15.
 - Delete files that is not needed for Dokploy (ex: *.sh and more).
 - Ready frontend - backend link (ex: API calls, environment variables, etc).
 - Github CI/CD workflow ready. (Using matrix methods to build both ARM64 and amd64 natively). Optimize for fastbest build.
 - Ready login page and register page.
 - Ready QR payment page.
 - Ready bank transfer page.
 - Ready dashboard page.
 - Ready profile page.
 - Ready logout page.
 - Ready 404 page.
 - Ready 500 page.
 - Ready 401 page.
 - Ready 403 page.
 - Ready 404 page.
 - Ready 500 page.