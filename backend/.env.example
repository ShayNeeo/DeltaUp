# Backend Environment Variables

# NOTE: These are RUNTIME variables.
# They are read by the application when it starts.
# You can change these in Dokploy/Docker environment settings
# without needing to rebuild the Docker image.

# Database connection (Dokploy will provide this)
DATABASE_URL=postgres://user:password@postgres:5432/deltaup

# JWT secret for token signing (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars

# Domain name (without https://)
DOMAIN=api.yourdomain.com

# Rust logging level
RUST_LOG=info

# Server port
PORT=8000

# CORS allowed origins (comma-separated, MUST include https:// for production)
# For local development:
# ALLOWED_ORIGINS=http://localhost:3000,https://localhost:3000
# For production with Cloudflare:
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
